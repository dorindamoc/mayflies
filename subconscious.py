"""Subconscious observer — stub. Not yet implemented.

When implemented, this will run after each heartbeat and reactive session,
monitoring civilization-level patterns across instances:
- Is heritage deepening or ossifying?
- Is proposal diversity real or convergent?
- Are adopted rules drifting from lived heritage?

The observer would write its assessment to memory/subconscious.md, which is
then injected into future system prompts as a <subconscious> block.
"""


def run_subconscious(trigger: str) -> None:
    """Run the subconscious assessment. Currently a no-op."""
    pass
